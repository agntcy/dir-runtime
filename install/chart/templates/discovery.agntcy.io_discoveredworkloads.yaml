---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: discoveredworkloads.discovery.agntcy.io
spec:
  group: discovery.agntcy.io
  names:
    kind: DiscoveredWorkload
    listKind: DiscoveredWorkloadList
    plural: discoveredworkloads
    shortNames:
    - dw
    - dws
    singular: discoveredworkload
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.runtime
      name: Runtime
      type: string
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.workload_type
      name: Type
      type: string
    - jsonPath: .spec.addresses
      name: Addresses
      type: string
    - jsonPath: .spec.ports
      name: Ports
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: DiscoveredWorkload is the Schema for the discoveredworkloads
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Information about a discovered workload in a runtime.
            properties:
              addresses:
                description: Addresses are the network addresses of the workload.
                items:
                  type: string
                type: array
              annotations:
                additionalProperties:
                  type: string
                description: Annotations are additional metadata.
                type: object
              hostname:
                description: Hostname is the hostname of the workload.
                type: string
              id:
                description: ID is the unique identifier for the workload.
                type: string
              isolation_groups:
                description: IsolationGroups are the network/namespace isolation groups.
                items:
                  type: string
                type: array
              labels:
                additionalProperties:
                  type: string
                description: Labels are key-value pairs for filtering.
                type: object
              name:
                description: Name is the human-readable name of the workload.
                type: string
              ports:
                description: |-
                  Ports are the exposed ports.
                  Specified as strings to accommodate various formats (e.g., "80/tcp", "443").
                items:
                  type: string
                type: array
              runtime:
                description: Runtime is the type of the runtime (e.g., docker, containerd).
                type: string
              services:
                description: |-
                  Services holds service/protocol-specific metadata.
                  These values can be used by discovery resolvers
                  to enrich the discovery information about the workload.
                properties:
                  a2a:
                    description: A2A holds metadata about the A2A service running
                      inside the workload.
                    x-kubernetes-preserve-unknown-fields: true
                  oasf:
                    description: OASF holds metadata about the OASF record that a
                      workload references.
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              type:
                description: Type is the type of workload (e.g., process, container,
                  pod).
                type: string
            type: object
          status:
            description: DiscoveredWorkloadStatus defines the observed state of DiscoveredWorkload.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the workload's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSeen:
                description: LastSeen is the timestamp when the workload was last
                  observed
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              phase:
                description: Phase indicates the current phase of the workload
                enum:
                - Discovered
                - Processing
                - Ready
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
